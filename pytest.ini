[pytest]
testpaths = tests/unit/backend tests/integration tests/system
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short
markers =
    slow: tests longs (deselect avec -m "not slow")
    integration: tests d'integration
    system: tests systeme bout-en-bout
