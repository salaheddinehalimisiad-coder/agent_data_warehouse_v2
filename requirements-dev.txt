# ============================================================
# Agent Data Warehouse v3.0 — Dépendances développement/tests
# Usage : pip install -r requirements-dev.txt
# ============================================================

# ── Inclure les dépendances de production ────────────────────
-r requirements.txt

# ── Tests ────────────────────────────────────────────────────
pytest>=8.0.0                     # framework de test
pytest-asyncio>=0.23.0            # support async/await dans les tests
pytest-cov>=5.0.0                 # rapport de couverture
httpx>=0.27.0                     # client HTTP pour tester FastAPI (TestClient)

# ── Qualité de code ──────────────────────────────────────────
ruff>=0.4.0                       # linter + formatter ultra-rapide (remplace flake8+black)
mypy>=1.10.0                      # vérification de types statiques

# ── Utilitaires dev ──────────────────────────────────────────
pre-commit>=3.7.0                 # hooks git (lint avant chaque commit)
